server:
  port: 9090

spring:
  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:ewm-stat-service-test
    username: ewm
    password: ewm
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: never